<% flash.each do |type, message| %>
  <div role="alert" class="alert <%= bootstrap_flash_for(type) %> fade in">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>
